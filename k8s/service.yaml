apiVersion: v1
kind: Service                                                             
metadata:                                                                 
  name: simon-argocd
  labels:
    app: simon-argocd
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: simon-argocd
