apiVersion: v1
kind: Service                                                             
metadata:                                                                 
  name: simon-argocd
  labels:
    app: simon-nginx
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: simon-nginx
